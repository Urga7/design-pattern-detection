# Copy to .env for the semantic run; docker compose reads it automatically.
# Only the key matching VERIFY_MODEL is needed.
GEMINI_API_KEY=
# VERIFY_MODEL=gemini-3.7-flash
# Fixed path makes repeat semantic runs free by reusing cached verdicts. Leave unset for the fresh cache.
# VERIFY_CACHE=/out/razsodbe.json
